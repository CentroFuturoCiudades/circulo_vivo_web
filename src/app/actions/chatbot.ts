"use server";

export async function getChatbotReply(message: string): Promise<string> {
  // Placeholder — wire to AI provider in next step
  return `Recibí tu mensaje: "${message}". Pronto tendrás una respuesta inteligente.`;
}
